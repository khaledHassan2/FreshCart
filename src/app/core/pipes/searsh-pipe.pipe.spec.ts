import { SearshPipePipe } from './searsh-pipe.pipe';

describe('SearshPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearshPipePipe();
    expect(pipe).toBeTruthy();
  });
});
